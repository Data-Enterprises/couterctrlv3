/** Moved to `src/components/KpiTileGrid.tsx` — Item Actions and LP Actions
 *  render the same band, and it was never a UPC-only component. Re-exported
 *  so the module panels' imports stay where they are. */
export { default } from "../../../../components-dev/KpiTileGrid";
