const Home = () => {
  return (
    <div className="w-full h-[calc(100vh-3rem)] p-4 flex items-center justify-center">
      This is where we can use prefs for nav and maybe some welcome message or
      any updates???
    </div>
  );
};

export default Home;
